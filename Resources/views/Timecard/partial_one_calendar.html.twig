{# <-- TODO Change the path to macros. Should not be localized in templatePath #}
{% import templatePath ~ "macros.html.twig" as macros %}


<table id="editableTable">
    {{  macros.lineCalendar(id, lineCalendar ) }}
</table>